# Runtime dependencies
requests>=2.31
python-dotenv>=1.0
# Official Polymarket CLOB Python client (v2). If pip cannot resolve this name,
# install directly from GitHub:
#   pip install git+https://github.com/Polymarket/py-clob-client-v2.git
# The bot imports this lazily so it is not required for dry-run / tests.
py-clob-client-v2>=0.1.0; python_version >= "3.9"

# Test dependencies
pytest>=7.4
